##-*- Mode: sh -*-
##---------------------------------------------------------------------------##
## .bashrc_ccs4_linux - my bash configuration file upon bash login
##---------------------------------------------------------------------------##

if test -n "$verbose"; then
   echo "In .bashrc_lu"
fi

##---------------------------------------------------------------------------##
## ENVIRONMENTS
##---------------------------------------------------------------------------##

# unlimit stack and core dump sizes.
ulimit -s unlimited

# Where is draco
_BINDIR=`dirname "$BASH_ARGV"`
export DRACO_MODULE_DIR=`(cd $_BINDIR/../Modules;pwd)`

# Where is the vendor directory
if test "${VENDOR_DIR}x" == "x"; then
   if test -d /usr/projects/draco/vendors; then
      VENDOR_DIR=/usr/projects/draco/vendors
   fi
fi

#
# MODULES
#
# Use this module command when running bash.
# function module() { eval `/usr/bin/modulecmd bash $*`; }
source /usr/share/Modules/init/bash
# Use modules found in the draco directory
# Clear any tcsh loaded modules as these are invalid when staring a new shell. 
module purge &> /dev/null
# Load a default set of modules.

module use $DRACO_MODULE_DIR/hpc
module use $DRACO_MODULE_DIR/tu-fe
module load friendly-testing 
module load intel/13.1.0 openmpi/1.6.3 # cudatoolkit
module load cmake gsl svn fstools 
module load numdiff lapack totalview
module load trilinos SuperLU_DIST/3.0-intel
module load ParMetis/3.1.1-intel ndi random123 eospac/v6.2.4beta.1-moonlight

# module load modules friendly-testing hpc-tools intel-c intel-f totalview
# module load openmpi-intel/1.4.3 grace python SuperLU_DIST/2.4-intel 
# module load lapack/atlas-3.8.3-intel BLACS/1.1-intel ParMetis/3.1.1-intel
# module load gsl/1.14-intel trilinos/10.6.4-intel SCALAPACK/1.7-intel
# module load hypre/2.0.0-intel grace-draco ndi xshow cmake numdiff

# Ensure that F90 and FC are set for Intel C/C++/FC environment.
#if test `echo $CC | grep icc | wc -l` = 1; then
#  export F90=ifort
#  export FC=ifort
#fi

##---------------------------------------------------------------------------##
## end of .bashrc_lu
##---------------------------------------------------------------------------##
